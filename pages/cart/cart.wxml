<!--pages/cart/cart.wxml-->
<view class='cart'>

  <bottom-bar selected="{{isSelectAll}}" 
              price="{{totalPrice}}"
              counter="{{totalCounter}}"
              bindtap="onSelectAll"/>
</view>